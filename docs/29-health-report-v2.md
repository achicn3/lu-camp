# 29 — 系統健檢報告 v2（180 天全真流程模擬）

> 執行日期：2026-07-16　依據：docs/27 計畫（四裁示已定）
> 基線：`main` @ `31b954a`　資料集：`lucamp_sim`（seed=20260716、可重現）
> 模式：report-first——本報告只記錄與分級，修復另開分支經裁示執行

---

## 1. 總結

**體質結論：健康。** 在 200 天、5,325 筆銷售、945 筆收購（全走手持切結流程）、746 筆
簽署任務的全真資料上，**帳務不變量 47/47、併發探針 9/9、66 個 API 端點 0 個 5xx、
13 條前端路由 0 個 JS 錯誤**；簽署證據鏈抽樣稽核 20/20 可完整重建；加密備份上傳 R2
並成功還原驗證。**未發現任何金額計算錯誤、資料損毀或 crash 級（P0）問題。**

發現集中在**可稽核性的最後一哩**（簽名調閱無稽核、無店長調閱 UI）與既知的
D-8 報表口徑（本次以真資料量化，影響 0.05%）。

## 2. 模擬資料集（sim_manifest.json 綁定，fail-closed）

| 項目 | 量 | 項目 | 量 |
|---|---|---|---|
| 模擬天數/現金班別 | 200/200 | 銷售（行） | 5,323（11,923） |
| 收購（買斷/寄售/散裝） | 645/260/40 | 簽署任務 | 741（切結/SCU/ACK） |
| 會員/寄售人 | 2,372 | 購物金分錄 | 226 |
| 採購單/收貨/進項發票 | 89/111/≈97 | 寄售結算（已付） | 260（251） |
| 盤點 | 8 | 退貨（含序號/寄售）/作廢 | 40/94 |

全流程經真實 service（無 raw-insert）：定價計算機、手持切結（簽名 PNG→綁定→撥款）、
現金班別逐日開關帳（5% 天故意差異＋原因）、活動 4 檔、購物金撥入/折抵/人工調整、
供應商改名＋停用（第 120 天）、部分到貨、發票補登、溢價率期中調整。
產生一次 2 分 41 秒，RNG 種子固定可重現。

## 3. 驗證結果

| 層 | 內容 | 結果 |
|---|---|---|
| A 四道門 | ruff/mypy、pytest（92.98% cov）、前端三門、合約漂移 | ✅ 全綠 |
| — migration | 全新庫 `alembic upgrade head` 從零到 head | ✅ |
| B 不變量 | 48 項（B1–B13＋S0–S9），含現金 200 班別逐一重算、點數逐筆重算、簽署證據全量（後端 PNG 驗證器重驗）、進項發票守恆、供應商名快照、DB 跨度實查綁定 | ✅ 48/48 |
| C 併發 | 雙台賣同序號品/散裝超賣/寄售雙付/同冪等鍵/同班別並發/購物金雙花/會員撞號/關帳競態/餐飲購物金防線 | ✅ 9/9 |
| D API | OpenAPI 掃描（誠實分桶：60 端點 2xx 運動到、4 個 4xx 屬合理、kiosk 2 個標記略過）＋F-1 naive 日期回歸（200）＋極端參數 | ✅ 0×5xx |
| E 瀏覽器 | 13 路由雙走查、0 pageerror/console.error/jargon、大表渲染、身分證防呆（表單 scope＋API 驗未寫入）、雙瀏覽器併發、CSV/xlsx 匯出 | ✅ |
| E+ UI 實操 | **POS 結帳（真 Amego）einvoice-smoke 19/19**：B2C 印證明聯/載具不印/B2B 帶統編/TOCTOU fail-closed 全案例，真開 ZA10011389–91 三張＋作廢一張（f0501，invoice_status=VOID）；**kiosk 手持簽署 13/13**：店員推送→平板手寫→綁定收購→現金/購物金撥款→憑證聯 payload→單次使用 409 | ✅ |
| F 效能 | 全端點 latency（最慢 191ms：reconciliation；naive 首打 808ms 屬冷快取單次）、DB 28MB、索引使用健康（idx≫seq） | ✅ 無超標 |
| 簽署稽核 | 抽 20 筆經 staff API 完整重建（內容快照＋影像 md5 與 DB 一致＋反查綁定單據） | ✅ 20/20 |
| 備份演練 | dump→AES 加密→R2（bucket pos）→下載→解密 sha256 一致→pg_restore→head＋簽名 sha256 20/20 | ✅（runbook＝docs/28） |

**本輪未涵蓋（誠實界定）**：EPSON 實機列印（agent 以 fake 模式驗 payload；實機四張驗印於
K6/#28 完成）。環境備忘：agent 需 `AGENT_BACKEND_URL`（抓店家抬頭）與 `AGENT_CORS_ORIGINS`
對齊前端埠，否則 `/print/acquisition` 503／瀏覽器擋列印。

效能基準已存 `backend/qa_e2e/api_scan_report.json`（供日後資料量成長時對照）。

## 4. 發現與分級

### P1（可稽核性缺口，建議近期修）

| # | 發現 | 證據 | 建議修法 |
|---|---|---|---|
| P1-1 | **調閱簽名影像不寫 audit_log**。`GET /signing/tasks/{id}/signature` 直接回 PNG，無稽核。簽名是個資級證據，依 CLAUDE.md §5「敏感查看留痕」精神應記錄誰在何時調閱 | `signing/router.py` 無 `write_audit_log`；S8 掃描 VIEW 類稽核 0 筆 | 端點加一筆 `SIGNATURE_VIEW` 稽核（actor/task/時間），約半天含測試 |
| P1-2 | **店長無法在 UI 調閱歷史簽名**。後端調閱端點存在，但前端無任何頁面消費——目前要看「某筆收購當時簽了什麼」只能由工程師下 SQL/curl，證據鏈在 UI 層斷掉 | 前端 grep 僅 kiosk 上傳引用 signature_image；無管理端調閱頁 | 收購/銷售明細加「簽署證據」檢視（內容快照＋簽名圖）；與 P1-1 同分支做，含瀏覽器 e2e |

### P2（已知口徑，等裁示——本次以真資料量化）

| # | 發現 | 量化（200 天模擬） | 說明 |
|---|---|---|---|
| P2-1 | D-8(1) 毛利報表不扣退貨（`margin_components` 只排 VOID） | 38 筆退貨、多計營收 **6,312 元＝全期 0.05%** | 退貨 UI 已擱置、僅 API 可退→現實發生率低。裁示選項：修口徑 vs 維持並記錄 |
| P2-2 | D-8(2) 退貨不沖會員點數 | 16 筆有買方退貨、涉及點數上限 **210 點** | 沖點口徑要裁示：按退款比例 vs 全退才沖 |

### P3（文件/工具，順手處理）

| # | 發現 | 狀態 |
|---|---|---|
| P3-1 | `docs/deferred-items.md` D-4 條目仍標「待排」，實際 2026-06-18 已解（core/deps 每請求重驗，本次亦驗證存在）；D-7(a)(b) 多被 Amego 路線取代 | 待整理該文件 |
| P3-2 | QA 檢查器 B6 口徑漏「退貨回補再售出」（誤報守恆破）；B13 需要人工調整分錄的資料覆蓋 | **本輪已修**（longrun_invariants B6 加退貨項；sim 引擎補 ADJUSTMENT 情境） |

### 已驗證為「系統正確、非缺陷」

- 散裝退貨回補→SOLD_OUT↔ON_SALE 狀態翻轉→再售出：庫存/狀態全一致（lot 35 全時間線重建）。
- F-1（trends naive 日期 500）已在 main 修復，本次回歸通過。
- 報表匯出 422 為缺 `from`/`to` 之正常驗證。
- 供應商改名不改寫歷史採購單（快照正確）、停用者建單被擋。

## 4.5 Codex 對抗審第一輪（2026-07-16）處置

3 P1＋7 P2 全數處置：runbook 上傳改精確檔名（P1）；時間平移改列序決定性偏移（P1，
updated_at≥created_at、同表順序＝建立順序；「舊列當日更新不平移」為文件化界限）；
F-1 邊界誤用參數別名致假通過→改 from/to＋斷言預期狀態（P1，重驗＝200）；歷史活動視窗
改為模擬期間真生效＋收尾回填（P2，折扣歷史行為真的被運動到）；退貨涵蓋序號/寄售品
（P2，結算反轉路徑有代表資料）；API 掃描誠實分桶＋kiosk 標記略過（P2）；S0 跨度改 DB
實查互核（P2）；helper 測試移入 tests/ 正式收集（P2）；瀏覽器防呆斷言 scope 到表單＋
API 驗未寫入（P2）；S1 改用後端 PNG 驗證器全量重驗（P2）。修正後：不變量 48/48、
API 0×5xx、瀏覽器 0 錯、真 Amego 19/19、kiosk 13/13。
連帶修正兩個檢查器口徑（B1/B6 未考慮退貨回補——經完整時間線重建確認系統本身正確）。

## 5. 建議修復波次

1. **波次一（P1-1＋P1-2，一個分支）**：簽名調閱寫稽核＋店長端簽署證據調閱 UI。
   TDD＋四道門＋Codex review＋瀏覽器 e2e 截圖。
2. **波次二（P2，先裁示）**：D-8 兩項口徑經店主決定後實作（工作量小；影響已量化，急迫性低）。
3. **順手**：deferred-items.md 文件整理（P3-1）。

## 6. 本輪新增資產（隨本報告一併入 repo）

- `backend/qa_e2e/sim_180d.py`＋`sim_helpers.py`＋`test_sim_helpers.py` — 180 天全真模擬引擎（可重現）
- `backend/qa_e2e/sim_invariants.py` — manifest fail-closed＋47 項不變量掃描
- `backend/qa_e2e/api_scan.py`＋`api_scan_report.json` — API 全掃描與效能基準
- `docs/27` 計畫、`docs/28` 備份 runbook、本報告
- 截圖：`/home/test/lu-camp-backups/fully-e2e-sim/`（13 路由＋防呆＋雙瀏覽器）
- R2 異地副本：`pos/backups/lucamp_sim_pristine_20260716.dump.enc`
