# 37 — 操作手冊產製計畫（修正版）

> 本文是「操作手冊產製任務指令」對照本專案**實際狀況**修正後的版本。
> 原始指令有三處會導致手冊寫錯、四處路徑名稱不符、一項與既有產線重複，逐一修正於此。
> 執行前必讀 `docs/34`（全系統重跑 Runbook）——本文延用其環境與腳本，不另建產線。

---

## 0. 店主裁示（2026-08-18）

| 項目 | 裁示 |
|---|---|
| 產線 | **沿用現有的**（`frontend/scripts/manual/`），腳本隨系統改動同步更新 |
| 實體列印 | **不用真機**：hardware-agent 走 Fake，手冊標註驗證層級為 Mock |
| 電子發票 | **全部要真的測**：開立、作廢（F0501）、折讓（G0401）都要走完平台 |
| 角色 | **三種角色完整寫**（MANAGER／CLERK／KIOSK） |
| 模擬資料 | **要，從頭寫**（取代既有 `seed_dev_demo.py`） |

---

## 1. 原始指令的錯誤與修正

### 1.1 會導致手冊寫錯（三項）

| 原始指令 | 實際狀況 | 修正 |
|---|---|---|
| 「系統僅有管理員一種角色」 | `UserRole` 有 `MANAGER`／`CLERK`／`KIOSK`。`KIOSK` 被 `get_current_user` 中央拒絕，只能用簽署端點；多處動作是 MANAGER-only（作廢銷售、改價、查看身分證、登記手開發票、手開紙本處置確認…），店員硬打 API 會 403 | 三種角色完整寫（§4） |
| 「硬體一律 Mock」 | 本機實際已接 EPSON TM-T82III（192.168.0.42）與 Brother QL-810W（192.168.0.45），`AGENT_DEVICES=real` | 依裁示走 Fake，但**手冊必須標註驗證層級**，不得寫成已實機驗證（§5.3） |
| 「einvoice_enabled = false」（全程停用） | `docs/34 §5` 整節在做真發票與後台核對；本次還要驗作廢與折讓 | **全域維持停用，但由腳本以 `withSettings` 暫時開啟**——既有三支腳本會在全域已啟用時中止（§2.1） |

### 1.2 路徑與名稱（改字即可）

| 原始指令 | 實際 |
|---|---|
| `lu_camp_manual` | **`lucamp_manual`**（前綴是 `lucamp`，不是 `lu_camp`） |
| `docker-compose.manual.yml` | 不存在；服務依 `docs/34 §3` 個別啟動 |
| `backend/scripts/seed_demo.py` | seed 一律在 **`backend/app/scripts/`** |
| 帳密「寫於 CLAUDE.md」 | CLAUDE.md 未載；實際 `dev-manager` / `dev-test-123456`（`seed_dev_user`） |
| `make manual-verify` | Makefile 存在但無 manual 目標；改用既有 `qa-manual.mjs` |

### 1.3 與既有產線重複（最大一項）

既有產線已具備原始指令 P2–P4 要求的全部能力：

| 原始指令要求 | 既有實作 |
|---|---|
| Playwright 擷取腳本 | `frontend/scripts/manual/` **30 支**，可重跑 |
| 內容分檔撰寫 | `content-1..5.mjs`（22 章） |
| base64 內嵌、單一 HTML | `convert-images.mjs`（WebP）＋ `build-manual.mjs` |
| RWD／離線／燈箱／搜尋／列印 QA | `qa-manual.mjs`（五種寬度、外部資源、離線開啟、列印樣式） |
| 產物 | 單一 HTML **5.6 MB**、進入手冊的截圖 **237 張**（`make-manifest.mjs` 的 PICK 合計；磁碟上另有未選用者） |

**修正：不重建 Python 產線。** 缺的只有兩項產物（`coverage.yaml`、`evidence.jsonl`），補進既有產線即可（§6）。

### 1.4 數值上限

原始指令的「截圖 ≤ 180 張」比現況（進入手冊的 **237 張**）還低，照做等於刪掉四分之一既有內容。

**修正**：硬性上限綁在**最終 HTML ≤ 12 MB**（現況 5.6 MB）與**單張 ≤ 80 KB**；張數改為原則——每個操作至少一張、同一操作不堆疊重複圖。

### 1.5 原始指令正確、予以保留

- `CategoryPricingRule` 確實存在（`inventory/models.py`）
- 簽名任務狀態確實含 `SIGNED`／`CONSUMED`／`VOIDED`／`EXPIRED`／`FAILED`，且**`SIGNED` 不是終態、`CONSUMED` 才是**
- 假資料安全規範（合成姓名、`0900-000-xxx`、`@example.com`、**刻意檢核碼錯誤**的身分證/統編、虛構地址、`is_seed` 旗標）——全數保留
- 固定亂數種子、`--purge`、環境保護——全數保留

---

## 2. 四個必須先解決的衝突

裁示彼此組合後產生的矛盾，先在此定案，避免執行到一半才發現。

### 2.1 大量資料 × 真發票

15,000 筆銷售若都開發票，等於送 15,000 張真發票到 Amego 測試環境——不可行也不該做。

**定案**：
- **P0 大量資料在 `einvoice_enabled = false` 下產生**，發票狀態一律 `NOT_ISSUED`
- 只有**手冊腳本要示範的少數幾筆**，由腳本**自行以 `withSettings` 暫時開啟再還原**
- 這符合 `CLAUDE.md §6`：不論開關，每一筆銷售都完整寫入 `sales`；開關只決定是否產生電子發票

> **絕對不要手動 PATCH 設定把 `einvoice_enabled` 開著**（審查 critical）。三支既有腳本
> （`09c-invoice-return-void`、`09d-invoice-disposition`、`17-einvoice-linepay`）開頭就會檢查，
> 發現全域已啟用即 **`throw`「疑似正式環境，中止」**，整輪當場停住。
> 反向也會壞：`08c-pos-consignment` 要截「未啟用電子發票時的提示」（`.pos-invoice-off`，
> 手冊圖 7-19），全域開著時該元素不存在，`makeShot` 會逾時或截到錯的畫面。
> 既有機制是 `_lib.mjs` 的 `withSettings(["einvoice_enabled"], …)`——沿用它，不要另闢蹊徑。

### 2.2 今日交易 × 「尚未開帳」畫面

原始指令 §3.3 要求今日有交易（儀表板不為空）；但今日有現金交易就必須開帳，而手冊腳本 `02-cash-open` 要示範**開帳**畫面（需「尚未開帳」狀態）。

**定案**：seed 今日交易後**把當日班別結掉**。今日資料留著、抽屜是關的，兩邊都成立。
（`docs/34` 已記載 `seed_dev_consignment` 有同樣問題，處置方式一致。）

### 2.3 平台識別碼碰撞（**必踩，已實測確認**）

送到 Amego 的識別碼由本地流水號推導：發票 `S{store}-{sale_id}`、折讓單 `L{store}-{allowance_id}`。
重建資料庫後編號從 1 重來，會撞上前幾輪重跑留在平台上的舊資料。實測結果：

| 識別碼 | 平台狀態 |
|---|---|
| `S1-1` | 已存在（ZA10018786，2026-07-23，$400） |
| `S1-14` | 已存在（ZA10028313，2026-08-03，$270） |
| `S1-20`、`S1-50` | 已存在 |
| `L1-1`、`L1-2` | 折讓單已存在 |

撞號的症狀是「對帳先行」查到舊發票 → 金額或時間對不上 → 判定結果不明 → **開立卡住**（502）。

**好消息：訊息會明講。** `amego.py` 的兩道守衛分別回
「…不符——**該紀錄可能不是本筆（識別碼重號）**，待人工對帳」與
「…**該紀錄是還原前的舊資料（識別碼重號）**，待人工對帳」。
看到「識別碼重號」就是這個問題，不必懷疑平台故障——**此句值得寫進手冊 FAQ**。

**定案**：migration 之後、seed 之前執行

```sql
ALTER SEQUENCE sales_id_seq RESTART WITH 300000;
ALTER SEQUENCE invoice_allowances_id_seq RESTART WITH 300000;
```

執行當天**先掃一次號段**確認未被佔用（Amego 測試帳號是公開共用的，別人也會用）。

**副作用**：restart 後銷售單號從 300001 起（六位數）。`docs/34 §5` 的
「訂單編號 `S1-14` = 銷售 #14」範例與既有手冊圖說的小號碼會全部過時，需同步更新。
另已查證**沒有第三個**由本地流水號推導的平台識別碼——LINE Pay 的 orderId 來自
（store, 冪等鍵, 金額），不受影響。

### 2.4 三種角色 × seed 帳號

三種角色完整寫，需要三個帳號才截得出畫面：

```bash
ALLOW_DEV_SEED=true SEED_USER_PASSWORD=dev-test-123456 \
  uv run python -m app.scripts.seed_dev_user                       # dev-manager / MANAGER
ALLOW_DEV_SEED=true SEED_USER_USERNAME=dev-clerk SEED_USER_ROLE=CLERK \
  SEED_USER_PASSWORD=dev-test-123456 uv run python -m app.scripts.seed_dev_user
ALLOW_DEV_SEED=true SEED_USER_USERNAME=dev-kiosk SEED_USER_ROLE=KIOSK \
  SEED_USER_PASSWORD=dev-test-123456 uv run python -m app.scripts.seed_dev_user
```

---

## 3. 環境（修正後）

| 項目 | 值 |
|---|---|
| 專案根目錄 | `/home/test/lu-camp` |
| 資料庫 | **`lucamp_manual`**（拋棄式；執行前確認名稱，否則中止） |
| 後端 | `http://localhost:8000` |
| 前端 | `http://localhost:3000` |
| 硬體代理 | `http://localhost:8001`（**前端預設值，零設定**），**不載入 `.env`** → 全 Fake |
| 帳號 | `dev-manager` / `dev-clerk` / `dev-kiosk`，密碼皆 `dev-test-123456` |
| 店家統編 | **`12345678`**（Amego 測試公司；不符會被平台直接回拒） |
| `einvoice_enabled` | **全程 `false`**——需要開立的腳本自行以 `withSettings` 暫時開啟並還原 |
| `AMEGO_APP_KEY` | **已備妥於 repo 根目錄 `.env`**（已被 `.gitignore` 排除）。啟動後端前先載入：`set -a && . /home/test/lu-camp/.env && set +a`。未載入則設定頁根本開不了發票 |
| `MANUAL_ALLOW_EINVOICE_ISSUE` | `true`（手冊腳本的「真的開發票」opt-in 閘門） |
| 版本錨定 | `git rev-parse --short HEAD` ＋ 分支名，寫入手冊首頁 |

> **金鑰位置的坑**：`backend/.env` 是**空檔（0 bytes）**，金鑰在**根目錄** `.env`。
> `docs/34 §2` 的 export 區塊既沒列 `AMEGO_APP_KEY`、也沒 source 根目錄 `.env`，
> 照著貼就會漏掉它——發票整條線會在「設定頁開不了發票」那裡卡住，而症狀看起來
> 像功能壞掉。docs/34 需同步補上這一行。
>
> 本測試環境的金鑰對應 Amego 公開測試公司（統編 `12345678`），2026-08-18 實測
> `invoice_query` 往返正常。**正式憑證絕不可寫入本文件或 repo 任何位置。**

**本輪刻意用 `:8001` 而非 `docs/34` 的 `:8787`**：前端預設就是 `:8001`，不必給
`NEXT_PUBLIC_AGENT_URL`，直接消滅「忘了給環境變數 → 列印與開錢櫃全部靜默失敗」那個坑
（`docs/34 §3`、§9 記載前兩輪都栽在這裡）。

**docs/34 需同步兩處**：埠號改 `8001`（或明示本輪偏離），
以及 `/devices/status` 的 `driver` 期望值由 `real` 改為 **`fake`**（本輪硬體全 Mock）。

### 3.1 破壞性操作

本環境為拋棄式，**所有破壞性操作必須實際執行並截圖**（作廢、退貨、退款、盤點調整、備份還原）。
不得以「避免污染資料」略過。執行前確認資料庫名為 `lucamp_manual`。

---

## 4. 角色（三種，完整寫）

| 角色 | 說明 | 手冊處理 |
|---|---|---|
| `MANAGER` | 管理者。可跨店彙整、解密 PII，並獨佔多項敏感動作 | 正文主視角 |
| `CLERK` | 門市店員。日常結帳、收購、退貨 | 每個 MANAGER-only 動作標「限店長」，並以 `dev-clerk` 截一組對照圖 |
| `KIOSK` | 手持簽署裝置專用身分。**被 `get_current_user` 中央拒絕**，只能用簽署端點，碰不到任何店務資料 | 顧客螢幕章節說明 |

**判定方法必須三向交叉**（只看 `ManagerDep` 會漏；審查 high）：

1. `ManagerDep` 相依
2. 全域 grep 內聯判定 `UserRole.MANAGER.value`——有三處店長閘門**不是** `ManagerDep`：
   `returns/router.py`（手開紙本處置確認）、`sales/router.py`（作廢的同名旗標）、
   `contacts/router.py`（national_id／roles 的服務層判定）
3. 前端導覽的 `managerOnly`（`app/(authed)/layout.tsx`）

**MANAGER-only 動作清單**（實作時再核一次）：
作廢銷售、作廢收購、改售價、現金手動調整、查看身分證、門市活動、餐飲菜單、報表、設定、
備份、發票佇列操作、**登記手開發票**、**手開紙本處置確認**、購物金人工校正、
庫存「詳細」（序號品／一般商品／散裝批）、分類目標毛利率與分類定價規則、
贈品／折扣原因代碼維護、簽名 PNG 待清理報表、LINE Pay 退款對帳與裁定、客顯付款對帳、溢價率歷史。

**兩個例外，手冊不得一律貼「限店長」**：

- **「變更角色」不是使用者角色**——本系統**沒有使用者管理 API 或畫面**（`user/router.py` 只有
  登入與 `/me`）。手冊第 20 章現有的「變更角色／身分證」指的是**會員的 `ContactRole`**，
  而且 CLERK 有「補登」例外（可為尚無 national_id 者設定、角色只增不減）。此條目需修正。
- **建立分類**是**條件式**：取決於 `settings.allow_clerk_manage_categories`
  （`inventory/router.py` 的 `_ensure_category_create_allowed`），手冊須標「依設定」。

---

## 5. 硬體與外部服務的驗證層級

裁示不用真機列印，**但手冊必須誠實標註**，不得寫成已實機驗證。

| 裝置／服務 | 本次驗證層級 | 手冊須說明 |
|---|---|---|
| EPSON TM-T82III（收據／明細聯／證明聯／出餐單） | **Mock** | UI 觸發流程、列印狀態顯示、**離線或缺紙時店員該怎麼辦** |
| Brother QL-810W 標籤機 | **Mock** | 同上（註：本機已接真機，本輪刻意不啟用） |
| 錢櫃 | **Mock** | 開啟觸發條件、與現金階段的關聯 |
| 出餐單第二台印表機 | **未接**（退回收據機） | 說明選配、未接時的行為 |
| 條碼掃描器 | 鍵盤輸入模擬 | 掃描欄位行為、掃到不存在條碼的提示 |
| 顧客螢幕 | **實際開第二個瀏覽器分頁** | 配對、即時同步、簽名流程 |
| 電子發票（Amego） | **真平台（測試憑證）** | 開立、作廢、折讓，並到後台核對 |

Mock 裝置的手冊重點放在**「裝置離線或失敗時店員該怎麼辦」**——那是日常最常出事的部分。

> **缺紙的 FAQ 怎麼寫**（審查指出原寫法本輪驗不到、且漏掉系統既有的補救入口）：
>
> - **主答案**：出餐單沒出來 → 到**交易紀錄 → 重印出餐單**（POS 失敗提示本身就寫
>   「請至交易紀錄重印」）。這是系統既有、隨時可用的補救動作。
> - **附註**：2026-08-18 單次實機觀察——缺紙時 API 立刻回 200、畫面顯示成功，
>   紙補回去後印表機會自動補印。屬印表機韌體緩衝行為，**本輪全走 Mock、未複驗**，
>   手冊須標明此來源，不得寫成本輪驗證結果。

---

## 6. 階段拆分與交接

原始指令的五階段保留，但**簽核方式改為：每階段結束由一個全新無記憶的 agent 審核**（店主裁示），
通過後才進下一階段。每階段獨立 commit。

```
P0 模擬營運資料      → seed_demo.py ＋ seed_verification.txt
P1 系統盤點與覆蓋清單 → docs/manual/coverage.yaml
PE 電子發票模組驗收   → einvoice-acceptance.mjs ＋ 平台回應證據鏈   ← 見 §11
P2 腳本更新與截圖     → screenshots manifest ＋ evidence.jsonl
P3 手冊內容           → content-*.mjs 更新（沿用既有 22 章結構）
P4 建置與品質檢查     → manual.html ＋ coverage-report.md ＋ qa-report.md
```

**PE 排在 P2 之前**（店主裁示 2026-08-18）：手冊要描述發票的作廢與折讓，
但那兩條路徑至今**從未被任何腳本送到平台**過。先把模組驗收完、確定行為，
再去寫手冊與截圖；順序反了會照著未驗證的行為寫。

### 6.1 既有產線缺的兩項產物（P1／P2 補上）

- **`docs/manual/coverage.yaml`** — 模組 × 頁面 × 操作 × 狀態（`verified`／`blocked`／`not_implemented`）
  × 對應章節。既有產線沒有這份機讀清單，只有第 22 章的人讀附錄。
- **`docs/manual/evidence.jsonl`** — 每列一筆：章節、步驟、截圖檔名、DOM 斷言、驗證時間。
  既有腳本已有斷言，只是沒有輸出成證據鏈。

### 6.2 停止條件（遇到立刻停下詢問）

- seed 不變條件驗證失敗
- 發現功能只存在於分支、未合併 `main`
- 最終 HTML 逼近 12 MB
- 需要修改應用程式原始碼才能繼續
- 需要實體硬體才能完成的步驟（本輪一律 Mock）
- Amego 測試帳號的號段被佔用或平台回應異常

---

## 7. P0：模擬營運資料（從頭寫）

### 7.1 取代既有腳本

既有 `backend/app/scripts/seed_dev_demo.py`（276 行，約 120 天大量現金銷售）與本階段重疊，
但**不可直接刪除**（審查 high）：`frontend/scripts/inventory-price-smoke.mjs` 的前置明載
「`lucamp_e2e` 已 seed（dev-manager ＋ `seed_dev_demo`）」。

**定案**：新增 `backend/app/scripts/seed_demo.py`；`seed_dev_demo.py` **保留**，
僅於 header 註明「已被 `seed_demo.py` 取代，仍供 `inventory-price-smoke` 使用」。

**沿用它已驗證的手法**：黃金路徑走 `SalesService.create_sale`（確保金額／毛利／庫存口徑正確），
再回填 `created_at` 鋪出時間序（報表一律以 `Sale.created_at` 篩選）。

**沿用時要避開的三個陷阱**（審查指出，既有腳本都有）：

1. 它直接寫入 `national_id_enc=f"enc-con-…"` ——**假密文**。照抄的話第 4 章
   「查看身分證字號」的解密端點會解不開，那張截圖拍不出來。必須走真正的加密路徑。
2. 它設 `monthly_fixed_cash_outflow=90000`，註解寫明「不設則每日儀表板『估算淨利』
   帶不出數字（N/A）」。新腳本漏這步，§7.10 的儀表板驗收就會有一格 N/A。
3. 它只開**一個**現金班別、全部銷售掛在其下再回填時間。§7.4 要 305–400 個歷史班別，
   **這一項不能沿用**，必須直接以 model 建立並回填時間，且 §7.5 #2 的驗證要 per-session 做。

### 7.2 產生方式（混合策略）

- **黃金路徑**必須經 domain service／應用層，確保狀態機、庫存流轉、現金抽屜、寄售結算等不變條件被真正觸發
- **量體資料**可 bulk insert 加速，但完成後必須跑**不變條件驗證**（§7.5）
- 驗證失敗即視為 seed 失敗，**不得帶著髒資料進入截圖階段**

### 7.3 時間軸與營運節奏

- 涵蓋**最近 12 個月至今日**，含**今日的部分交易**（今日班別最後要結掉，見 §2.2）
- 營業日約 305 天；農曆春節連休 4–5 天；另 2–3 個颱風休業日
- **季節性（台灣露營）**：裝備類 10 月–翌年 2 月旺季，6–8 月淡季（約旺季 40%）；飲品夏季反向上升
- **收購來源季節性**：3–5 月（露季結束出清）與 1 月（年前整理）為送件高峰
- **週間**：週六日約平日 2.5–3 倍
- **時段**：飲品上午小高峰；裝備 14:00–18:00，假日 13:00–17:00 尖峰

### 7.4 資料量目標

| 類型 | 目標 | 類型 | 目標 |
|---|---|---|---|
| 銷售交易 | 12,000–15,000 | 二手商品個體 | 8,000–10,000 |
| 銷售明細 | 22,000–28,000 | 期末在庫 | 900–1,300（含 150+ 滯銷 180 天以上） |
| 作廢／更正 | 120–200（約 1%） | 散裝批次 | 150–250 |
| **退貨**（含部分退／整筆退） | **200–350** | | |
| 會員 | 2,000–2,500 | 新品 SKU | 60–120 |
| 非會員聯絡人 | 700–1,000 | 飲品／餐點 SKU | 25–40 |
| 買斷收購 | 1,800–2,400 | 現金工作階段 | 305–400 |
| 寄售合約 | 500–700 | 現金差異事件 | 15–30（含 1–2 筆大額） |
| 購物金異動 | 400–800 | 簽名任務 | 400–900 |

> 原始指令稱「儲值金」，本系統的正式名稱是**購物金**（store credit）。手冊一律用後者。

### 7.5 不變條件驗證（失敗即中止）

至少檢查：

逐條對應 `CLAUDE.md §7` 的領域核心不變量（審查指出原清單漏了五條、且弱化了一條）：

| # | 不變量 | CLAUDE.md §7 |
|---|---|---|
| 1 | 序號品一旦 `SOLD` 不可再售或重複入庫 | 1 |
| 2 | **等式**：應付寄售人 ＝ 售價 − 抽成；抽成 ＝ `round_ntd(售價 × commission_pct / 100)`，並產生 `PENDING` 結算 | 2 |
| 3 | 買斷毛利 ＝ 售價 − 收購成本；寄售只認抽成、不認全額售價 | 3 |
| 4 | 現金：開帳零用金 ＋ 銷售現金收入 − 收購付出 − 寄售付款 ± 手動調整 ＝ 結帳應有現金（**per-session**） | 4 |
| 5 | 已開票的退貨必須產生折讓單，**不得**直接刪除發票 | 5 |
| 6 | 散裝批 `remaining_qty` 不得 < 0；歸零轉 `SOLD_OUT`；每件成本 ＝ 收購成本 ÷ 總件數 | 6 |
| 7 | 退已售寄售品須反轉結算（未付→`CANCELLED`；已付→`reclaim_needed`） | 7 |
| 8 | 影響現金的操作必須在開帳中的 `cash_session` 下 | 8 |
| 9 | 定價 `margin_pct` 限 0–99（≥100 會除以零／負值） | 9 |
| 10 | 購物金餘額 ＝ 異動流水加總 | — |
| 11 | 每筆銷售明細金額加總 ＝ 交易總額 | — |

> #2 原本寫成「撥付 **≤** 售出金額」——不等式驗證不出漏算抽成以外的任何錯誤，改回等式。
> #7 尤其重要：§7.7 要求「未售退回」情境，沒有這條驗證等於放它過。

輸出 `seed_verification.txt`。

### 7.6 商品與價格

- 二手裝備長尾：眾數 NT$300–1,500，長尾至 NT$15,000–30,000
- 飲品 NT$60–180；新品 NT$150–3,000
- 分類涵蓋：帳篷、天幕、桌椅、睡袋睡墊、炊具爐具、燈具、收納推車、保冷保溫、服飾、配件零件、兒童用品
- **收購價須符合 `CategoryPricingRule` 矩陣與現行定價規則**；不符者不得寫入
- 未達 NT$300 轉售價值者走散裝批次——**這是 seed 內部的啟發式，不是系統規則**
  （E 級的定義是「秤斤／整袋收」，系統沒有任何金額門檻），**不得寫入手冊**，
  否則就是捏造營運規則

### 7.7 各流程必須涵蓋的狀態（每項至少數十筆）

- 庫存狀態機全部節點（在庫、已售、退回、報廢、滯銷）
- 寄售生命週期：合約成立 → 售出 → 結算撥付 → 未售退回 → 逾期未取
- 簽名任務：`SIGNED`、`CONSUMED`、`VOIDED`、`EXPIRED`、`FAILED` 各須存在
  （注意 `SIGNED` 非終態，必須有被單據 `CONSUMED` 的樣本）
- 現金抽屜：正常結平、短溢、零用金支出、買斷現金支出
- 會員分佈：約 60% 單次、30% 偶發（2–5 次）、10% 常客（6–40 次）；前 10% 貢獻約 40% 裝備營收

### 7.8 假資料安全（強制）

- 姓名用固定合成名單，不得取自任何真實來源
- 電話統一 `0900-000-000`–`0900-000-999`（未配發號段）
- 電子郵件一律 `@example.com`
- **身分證字號與統一編號必須刻意產生檢核碼錯誤的值**，確保永不與真實對象碰撞
- 地址用虛構門牌
- 每筆帶 `is_seed` 旗標，供一鍵清除

### 7.9 可重現與安全

- 位置：`backend/app/scripts/seed_demo.py`
- `--seed <int>` 固定亂數種子，相同種子必須產生完全相同的資料集
- `--purge` 完整清除
- 環境保護：`APP_ENV` 非 development/test、或資料庫名**不在允許清單**
  （`lucamp_manual`／`lucamp_e2e`）時**直接中止**。硬綁單一名稱會讓它在 e2e 庫上拒跑
- 需 `ALLOW_DEV_SEED=true` 明確 opt-in（沿用既有 seed 慣例）

### 7.10 P0 驗收

- 不變條件驗證全過，輸出 `seed_verification.txt`
- 儀表板、報表、庫存、會員、現金各主要頁面皆非空白且分頁超過一頁
- 記錄主要清單頁與報表查詢回應時間；> 1s 者列入待改善清單

---

## 8. P1–P4 摘要（沿用既有產線）

### P1 盤點
從**原始碼與實際網站雙向盤點**（前端路由與選單、後端路由與權限裝飾器、實際逐一點開）。
產出 `docs/manual/coverage.yaml`。可分模組並行，各自寫入 `docs/manual/coverage.d/` 再合併，
**不得多個 agent 寫入同一檔案**。

### P2 腳本更新與截圖
既有 30 支腳本需隨系統改動更新，另**新增 2 支**：

- `08g-pos-dinein.mjs` — 內用／外帶切換、桌號、結帳出餐單；設定頁桌號清單與開關（docs/35）
- `09e-manual-paper-invoice.mjs` — 只看未開立 → 登記手開發票 → 列表「手開紙本」標記 → 退貨／作廢的紙本確認（docs/36）

> **不需製造平台錯誤**：開立是前端觸發的（`POST /sales` 只建 `PENDING` 發票，
> 送平台是 POS 另外呼叫 `/einvoice/sales/{id}/issue`，且該端點**非** ManagerDep）。
> 腳本用 API 建一筆不呼叫開立的單，就自然停在「未開立」，正是登記手開發票所需的狀態。
>
> **但要註明涵蓋度缺口**（審查 medium）：這樣合成的單 `posted_at IS NULL` 且 `attempts = 0`
> → `_may_have_reached_platform()` 為 false → `register_manual_invoice` **整段跳過平台對帳查詢**。
> 也就是手冊示範的是「從未送出」那條窄路，最有價值的守衛（「平台上已經有這筆交易的發票…」）
> 沒被觸發。此缺口寫入第 22 章附錄；真正的守衛驗證由 **PE 階段第 8 項**負責。

錯誤路徑**必須實際觸發並截圖**，範圍限**表單層錯誤**（必填未填、格式錯誤、狀態不允許）。
**平台層失敗不在此列**：重現它需要「統編故意不是 12345678」，與 §3 規定的統編直接衝突
（`pos-invoice-failure-smoke.mjs` 開頭已載明此前提）。
新增、編輯、刪除、查詢、審核、匯入、匯出**分別撰寫**，不得合併帶過。

### P3 內容
沿用既有 `content-1..5.mjs` 的 22 章結構，補入新章節內容（見 §9）。

### P4 建置與 QA
`make-manifest` → `convert-images` → `build-manual` → `qa-manual`。
驗收未過不得宣稱完成。

> **新增腳本後必須同步兩個硬編清單**（審查 low，漏了會缺圖）：
> `docs/34 §4` 的 for 迴圈腳本清單，以及 `make-manifest.mjs` 的 `PICK`。
> `build-manual.mjs` 缺圖時只印「⚠ 缺圖」**不會中止**，很容易矇混過去。

---

## 9. 本輪要補進手冊的內容

新功能不另開章節，插進既有章節（手冊按**畫面**分章，非按規格文件）：

| 章節 | 補什麼 | 來源 |
|---|---|---|
| 7 POS 結帳 | 內用／外帶切換、選桌號、結帳後自動出餐單 | docs/35 |
| | **外帶餐飲同樣不累點、不折扣、不可用購物金**（店員最常問） | docs/35 |
| | 發票開立失敗時畫面說什麼、「重試開立」在哪——**本輪無新截圖**，沿用 `pos-invoice-failure-smoke` 既有證據（理由：統編衝突，見 §8） | docs/36 |
| 8 交易紀錄 | 桌號欄、「重印出餐單」 | docs/35 |
| | 「只看未開立發票」篩選（開立失敗的單唯一找得回來的途徑）、載入更多 | docs/36 |
| | 「登記手開發票」對話框與號碼格式 | docs/36 |
| | 列表的「手開紙本」標記（月底申報要分開處理） | docs/36 |
| | 手開紙本的退貨與作廢：店長確認紙本已處置 | docs/36 |
| 11 餐飲菜單 | 交叉引用第 7 章的內用流程 | docs/35 |
| 5 顧客螢幕 | KIOSK 角色說明：長駐登入、**僅能**用簽署端點、碰不到店務資料 | §4 |
| 20 管理員功能總表 | **角色標示的真正落點**：補上登記手開發票、手開紙本處置確認、購物金人工校正等；並修正現有的「變更角色」條目（見 §4） | §4 |
| 17 設定 | 桌號清單維護、出餐單列印開關 | docs/35 |
| 19 跨系統流程 | 新增第五條：內用點餐 → 桌號 → 結帳出餐單 → 加點 → 退貨 | docs/35 |
| 21 常見問題 | 出餐單沒印出來 → 補紙後會自動印（實測結論） | 實測 |
| 22 覆蓋附錄 | 更新 POS／交易紀錄／電子發票三列，並標註硬體本輪為 Mock | 兩者 |

全書另需補**角色標示**：每個 MANAGER-only 動作加「限店長」，並以 `dev-clerk` 截對照圖（§4）。

---

## 10. 文字風格（沿用原始指令）

- 繁體中文（台灣用語）
- 讀者不一定熟悉系統，避免不必要的技術術語；專有名詞第一次出現加簡單說明
- 每個步驟只描述一個主要操作
- 按鈕、欄位、選單名稱**必須與畫面完全一致**，不得自創
- **禁止**「依畫面指示操作」「填寫相關資料」「完成必要欄位」等模糊描述
- 每個功能說明統一格式：功能名稱／用途／前置條件／進入路徑／畫面說明／操作步驟／
  操作結果／系統連動／注意事項／常見問題／對應截圖


---

## 11. PE：電子發票模組驗收（對真平台，排在 P2 之前）

店主裁示：**發票相關 API 要全部測過**。本階段獨立於手冊產製，先確認模組行為，
再據以撰寫手冊。

### 11.1 為什麼要獨立一步

盤點既有腳本後發現一個空白：**沒有任何腳本呼叫過 `POST /einvoice/queue/{id}/send`**。
也就是說作廢（F0501）與折讓（G0401）的整條送出路徑，至今只有人工在後台按過
（`docs/34 §5` 即要求店長手動觸發）。手冊卻要描述這兩條流程。

### 11.2 API 面清單（查證自 `einvoice/router.py`、`einvoice/amego.py`）

**HTTP 端點（6 支）**

| 端點 | 用途 | 既有覆蓋 |
|---|---|---|
| `GET /einvoice/queue` | 佇列列表（狀態過濾、分頁） | 已被讀取驗證 |
| `POST /einvoice/sales/{id}/issue` | 開立（冪等） | B2C／載具／B2B 已對真平台驗過 |
| `POST /einvoice/sales/{id}/manual-invoice` | 手開紙本登記 | 有腳本，**未對真平台**驗證 |
| `POST /einvoice/queue/{id}/send` | 送出（ISSUE／VOID／ALLOWANCE 共用出口） | **無** |
| `POST /einvoice/queue/{id}/retry` | 重試（FAILED → PENDING） | **無** |
| `POST /einvoice/queue/{id}/result` | 記錄回執 | **無** |

**平台端點（5 支）**：`/json/f0401`、`/json/f0501`、`/json/g0401`、
`/json/invoice_query`、`/json/allowance_query`。後三者除了「開立前對帳先行」之外，
沒有針對性驗證。

**開立變體（4 種，由 `build_f0401_data` 分支決定）**：
B2C 一般（`BuyerIdentifier=0000000000`、`TaxAmount=0`）、B2B 統編（用發票落地快照的
net/tax 分稅）、手機載具（`CarrierType`／`CarrierId1`／`CarrierId2`）、捐贈（`NPOBAN`）。
**捐贈那條只在 UI 欄位出現過，從未真的開出一張捐贈發票。**

### 11.3 驗收矩陣（`frontend/scripts/einvoice-acceptance.mjs`）

比照既有的 `linepay-acceptance.mjs`（同樣是「對真平台把一個整合的 API 走完」）。
以 API 為主、不走瀏覽器——UI 部分既有 `einvoice-smoke.mjs` 已覆蓋。

1. **開立四變體**：B2C／B2B／載具／捐贈，各取得號碼與隨機碼
2. **冪等**：對同一筆再呼叫 `issue`，必須回原發票且**不得**再送平台
3. **對帳先行**：以 `invoice_query` by `order_id` 查得到剛開的那張，金額相符
4. **作廢**：同月整筆退貨 → 排 F0501 → **真的 send** →
   以 `invoice_query` by `invoice_number` 確認平台狀態為已作廢
5. **折讓**：部分退貨 → 排 G0401 → **真的 send** →
   以 `allowance_query` 確認折讓單存在，且折讓單號、原發票號、稅額與含稅總計相符
6. **已折讓後再退貨** → 必須繼續折讓，**不得**作廢原發票
7. **佇列端點**：`retry`（FAILED → PENDING）、`result`（記錄回執）
8. **手開紙本**：登記後 ISSUE 佇列轉 `CANCELLED`；三個出口（F0501／G0401／退貨決策）
   全部擋下；並以 `invoice_query` 確認**平台上真的沒有那張**
9. **銷售作廢** → 連動發票作廢

每一步保留平台回應的原始碼與號碼，作為證據鏈輸出。

### 11.4 前置與風險

- **序號推高是前提**（§2.3）：折讓單號 `L1-1`、`L1-2` 已被佔用，不推高會直接撞號
- 本階段會在 Amego 測試後台留下**真的發票、作廢與折讓紀錄**；測試環境無妨，
  但號碼會真的被消耗，不可無限量重跑
- 執行前先掃號段確認未被佔用（公開共用測試帳號）

### 11.5 PE 驗收

- 矩陣九項全綠，輸出平台回應證據
- 在 Amego 後台以肉眼核對至少：一張開立、一張作廢、一張折讓
- 任何一項失敗即停下回報，**不得**帶著未驗證的行為進入 P2 撰寫手冊
