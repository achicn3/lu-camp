// 店名／系統名稱的**唯一出處**。
//
// 這幾個字原本寫死在三個檔案裡（瀏覽器分頁標題、登入頁、顧客螢幕待機畫面），
// 改店名時很容易漏掉其中一處。**注意這與資料庫的 `stores.name` 是兩回事**：
// 那個是印在發票／收據抬頭上的營業人名稱，由後端提供；這裡是畫面上的招牌字。
// 兩者通常一致，但改一邊不會自動改另一邊。

/** 顧客看得到的店名（顧客螢幕待機畫面）。 */
export const STORE_DISPLAY_NAME = "露坑選物露營用品";

/** 店員端的系統名稱（瀏覽器分頁、登入頁標題）。 */
export const APP_TITLE = `${STORE_DISPLAY_NAME} POS`;
